---
description: Show CIA's dependency graph for the project
---

1. Call the `cia_graph` MCP tool with `output_format=text` to get a readable graph.

2. Present the graph showing each module, what it imports, and what uses it.

3. Report the total number of modules and edges.

4. If CIA detected circular dependencies, highlight them.
