---
description: Initialize CIA in the current project
---

1. Call the `cia_init` MCP tool to create a `.ciarc` configuration file.

2. Report whether the file was created or already existed.

3. Suggest the user can customize it with `cia_config_set` or by editing `.ciarc` directly.
